% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/integration_api.R
\name{read_aofm_file}
\alias{read_aofm_file}
\title{Parse a local AOFM workbook}
\usage{
read_aofm_file(file_path, table_id, csv = FALSE)
}
\arguments{
\item{file_path}{Path to an existing local AOFM \code{.xls} or \code{.xlsx} workbook.
The file is not copied or removed.}

\item{table_id}{A stable parser-supported \code{table_id} returned by
\code{\link[=aofm_catalog]{aofm_catalog()}}. The ID selects the workbook-family parser and must match
the local file's layout.}

\item{csv}{Logical scalar (default \code{FALSE}). When \code{TRUE}, also writes the
parser's legacy CSV output below \verb{output/} in the current working
directory. Leave \code{FALSE} for a read-only parse.}
}
\value{
The same family-specific data-frame or named-list result as
\code{\link[=read_aofm]{read_aofm()}}, with an \code{aofm_metadata} attribute containing local
byte-level provenance. Long-form results commonly contain identifier/date
or period columns, \code{name}, and \code{value}; exact measure columns and list
components follow the selected AOFM workbook family.
}
\description{
This function separates retrieval from parsing so callers can retain and
hash an immutable raw workbook before normalising its observations. It is
deterministic for a fixed local workbook and never contacts the AOFM
website.
}
\details{
This local-file API performs no network request and does not use or populate
the managed \code{.readAOFM} cache. Use \code{\link[=download_aofm_file]{download_aofm_file()}} first when a
verified managed retrieval is desired. Unsupported/raw-only catalogue rows,
missing files, invalid IDs, and layouts that do not satisfy the selected
parser's contract throw errors. With \code{csv = TRUE}, the explicit \verb{output/}
write is the parser's legacy side effect; it is separate from managed cache
storage.
}
\examples{
fixture <- system.file("extdata", "tb_issuance.xlsx", package = "readAOFM")
if (nzchar(fixture)) {
  issuance <- read_aofm_file(fixture, "tb_issuance")
  utils::head(issuance[, c("date_held", "name", "value")])
  attr(issuance, "aofm_metadata")[c("table_id", "raw_sha256")]
}
}
\seealso{
\code{\link[=download_aofm_file]{download_aofm_file()}} for managed retrieval,
\code{\link[=aofm_file_metadata]{aofm_file_metadata()}} for standalone provenance, and \code{\link[=read_aofm]{read_aofm()}} for
the legacy download-and-parse workflow.
}
