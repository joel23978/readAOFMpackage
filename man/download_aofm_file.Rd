% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/integration_api.R
\name{download_aofm_file}
\alias{download_aofm_file}
\title{Download one AOFM workbook by stable table ID}
\usage{
download_aofm_file(
  table_id,
  path = tempdir(),
  overwrite = TRUE,
  timeout = getOption("readAOFM.timeout", 30),
  retries = getOption("readAOFM.retries", 1L),
  max_bytes = getOption("readAOFM.max_bytes", 100 * 1024^2),
  max_age = getOption("readAOFM.max_age", 7 * 24 * 60^2),
  max_files = getOption("readAOFM.max_files", 100L),
  max_cache_bytes = getOption("readAOFM.max_cache_bytes", 500 * 1024^2),
  lock_timeout = getOption("readAOFM.lock_timeout", 10)
)
}
\arguments{
\item{table_id}{A stable, parser-supported \code{table_id} returned by
\code{\link[=aofm_catalog]{aofm_catalog()}}. Raw-only/unsupported rows are rejected because no local
parser contract exists for them.}

\item{path}{Root directory selected by the caller for the managed cache
(default \code{tempdir()}). The function creates
\verb{.readAOFM/data/<table_id>/} below this root; it does not write to the
package installation, the global workspace, or the user's home directory
unless that location is explicitly supplied as \code{path}.}

\item{overwrite}{Logical scalar (default \code{TRUE}). Download and verify the
current workbook even when a valid content-addressed cache entry exists.
Set \code{FALSE} to reuse a valid entry within \code{max_age}.}

\item{timeout}{Per-attempt HTTPS transport timeout in seconds (default from
\code{getOption("readAOFM.timeout", 30)}).}

\item{retries}{Non-negative number of retries after the first transport
attempt (default from \code{getOption("readAOFM.retries", 1L)}).}

\item{max_bytes}{Maximum accepted workbook size in bytes (default from
\code{getOption("readAOFM.max_bytes", 100 * 1024^2)}).}

\item{max_age}{Maximum age of a cache entry in seconds when
\code{overwrite = FALSE} (default from \code{getOption("readAOFM.max_age", 7 * 24 * 60^2)}). Expired entries are refreshed.}

\item{max_files}{Maximum number of content-addressed workbooks retained
below the selected cache root (default from
\code{getOption("readAOFM.max_files", 100L)}).}

\item{max_cache_bytes}{Maximum combined byte size of retained workbooks
(default from \code{getOption("readAOFM.max_cache_bytes", 500 * 1024^2)}).}

\item{lock_timeout}{Maximum seconds to wait for another writer for the same
table (default from \code{getOption("readAOFM.lock_timeout", 10)}).}
}
\value{
A normalized character path to the verified workbook. The returned
path has \code{table_id}, \code{source_url}, \code{raw_sha256}, \code{raw_bytes},
\code{retrieved_at}, \code{cache_hit}, and \code{aofm_metadata} attributes. The metadata
list records the schema version, source filename, content-addressed cache
filename, exact byte count, SHA-256 digest, and UTC retrieval time.
}
\description{
This is the explicit, managed-download API. It retrieves one current AOFM
workbook over HTTPS, verifies its size and SHA-256 digest, and stores it in
a content-addressed cache. Use \code{\link[=read_aofm_file]{read_aofm_file()}} to parse a retained local
file without another network request.
}
\details{
Each table cache contains a SHA-256-named \code{.xls}/\code{.xlsx} file, a
\code{current.rds} metadata record, and a short-lived writer lock. Successful
downloads prune old entries according to \code{max_age}, \code{max_files}, and
\code{max_cache_bytes}; the current verified file is retained. Temporary staging
files and atomic metadata replacement prevent partial downloads from being
exposed. The managed cache is opt-in through this function; the legacy
\code{\link[=download_aofm_xlsx]{download_aofm_xlsx()}} and \code{\link[=read_aofm]{read_aofm()}} workflows retain their existing
temporary staging and explicit \verb{data/}/\verb{output/} side effects.

Invalid table IDs, unsupported rows, unsafe paths or bounds, lock timeouts,
HTTP/transport failures, non-workbook responses, oversized files, and
failed integrity checks throw errors. No credentials are required. AOFM
controls the remote URL and workbook layout, so a source change can require
a package update.
}
\examples{
# Downloading is deliberately interactive because it contacts the live
# AOFM website. The bounds keep an example transfer finite.
if (interactive()) {
  cache_root <- tempfile("readAOFM-cache-")
  workbook <- download_aofm_file(
    "tb_issuance",
    path = cache_root,
    overwrite = FALSE,
    timeout = 5,
    retries = 0,
    max_bytes = 50 * 1024^2,
    max_age = 24 * 60^2,
    max_files = 2,
    max_cache_bytes = 100 * 1024^2,
    lock_timeout = 5
  )
  c(path = workbook, sha256 = attr(workbook, "raw_sha256"))
}
}
\seealso{
\code{\link[=aofm_catalog]{aofm_catalog()}} for offline discovery, \code{\link[=aofm_file_metadata]{aofm_file_metadata()}} for
local provenance, \code{\link[=read_aofm_file]{read_aofm_file()}} for offline parsing, and
\code{\link[=download_aofm_xlsx]{download_aofm_xlsx()}} for the legacy explicit \verb{data/} download workflow.
}
