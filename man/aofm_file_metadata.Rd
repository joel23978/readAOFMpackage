% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/integration_api.R
\name{aofm_file_metadata}
\alias{aofm_file_metadata}
\title{Exact provenance for a local AOFM workbook}
\usage{
aofm_file_metadata(file_path, table_id = NULL)
}
\arguments{
\item{file_path}{A path to one existing local \code{.xls} or \code{.xlsx} workbook.
The file is read to compute its byte count and SHA-256 digest.}

\item{table_id}{Optional parser-supported stable AOFM table ID. Supply it
when checking a standalone local file; a managed-cache path can carry the
table ID in its download metadata.}
}
\value{
A named list with \code{table_id}, \code{source_url}, \code{source_filename},
numeric \code{raw_bytes}, lowercase hexadecimal \code{raw_sha256}, UTC POSIXct
\code{retrieved_at}, \code{package_version}, and logical \code{cache_hit}. For a
standalone local file, \code{source_url} is \code{NA} and \code{retrieved_at} is the
file's modification time; managed-cache provenance is retained only when
its metadata and bytes still agree.
}
\description{
Computes byte-level provenance without downloading or modifying the
workbook. It can inspect a packaged fixture, an arbitrary local workbook,
or a path returned by \code{\link[=download_aofm_file]{download_aofm_file()}}.
}
\details{
This function performs no network request, creates no cache, and has no
output side effect. Invalid paths, unsupported file extensions, malformed
managed-cache metadata, and unknown table IDs are reported as errors or as
unqualified local provenance rather than being silently treated as current
AOFM data.
}
\examples{
fixture <- system.file("extdata", "tb_issuance.xlsx", package = "readAOFM")
if (nzchar(fixture)) {
  metadata <- aofm_file_metadata(fixture, table_id = "tb_issuance")
  metadata[c("table_id", "source_filename", "raw_bytes", "raw_sha256")]
}
}
\seealso{
\code{\link[=download_aofm_file]{download_aofm_file()}} for verified managed retrieval and
\code{\link[=read_aofm_file]{read_aofm_file()}} for parsing a local workbook.
}
