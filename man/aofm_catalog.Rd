% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/integration_api.R
\name{aofm_catalog}
\alias{aofm_catalog}
\title{List AOFM data tables known to the package}
\usage{
aofm_catalog(include_unsupported = FALSE)
}
\arguments{
\item{include_unsupported}{Logical scalar (default \code{FALSE}). Include the
seven catalogue rows without a package parser when \code{TRUE}.}
}
\value{
A base \code{data.frame} with one row per selected table and these
columns: \code{security}, \code{type}, \code{table_id}, \code{reader}, \code{category}, \code{title},
\code{description}, \code{source_url}, \code{file_name}, and logical \code{supported}.
Character columns are returned as character vectors, not factors. The
\code{source_url} values for parser-supported rows are the current HTTPS AOFM
workbook routes used by the package; raw-only rows are retained for
catalogue continuity and may use their verified raw-download routes.
}
\description{
Returns the package's local table catalogue without downloading a workbook
or contacting the AOFM website. Stable \code{table_id} values can be passed to
\code{\link[=download_aofm_file]{download_aofm_file()}} and \code{\link[=read_aofm_file]{read_aofm_file()}}.
}
\details{
The catalogue contains 23 parser-supported rows by default. Seven
additional rows are retained as raw-only/unsupported source records and can
be included for catalogue or raw-download workflows with
\code{include_unsupported = TRUE}; they are not accepted by the parser APIs.

Catalogue lookup is deterministic and offline. It does not validate a URL
by making a network request. A non-missing logical scalar is required for
\code{include_unsupported}; invalid values throw an error.
}
\examples{
catalog <- aofm_catalog()
catalog[, c("table_id", "reader", "supported")]

all_catalogue_rows <- aofm_catalog(include_unsupported = TRUE)
table(all_catalogue_rows$supported)
}
\seealso{
\code{\link[=search_aofm]{search_aofm()}} for text search, \code{\link[=download_aofm_file]{download_aofm_file()}} for the
opt-in managed download/cache workflow, and \code{\link[=read_aofm_file]{read_aofm_file()}} for parsing
a local workbook.
}
